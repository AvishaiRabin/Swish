import React from "react";
import { createRoot } from "react-dom/client";
import CourtsideApp from "../Courtside.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CourtsideApp />
  </React.StrictMode>
);
